(function($){
	$(function() {
		// var ref = $('.main__conditions_menu li');
		// var item = $('.post_item');
		// var def = false;

		// for (var i = 0; i < item.length; i++) {
		// 	if( item[i].css('display') == 'block' )
		// 		def = true;
		// }

		// if ( def == false ) {
		// 	item[0].css('display') == 'block'
		// }

		// var bord = {};
		// for (var i = 0; i < item.length; i++) {
		// 	bord[ref[i]] = item[i];
		// }

		// for (var i = 0; i < bord.length; i++) {
		// 	if (bord[i].classList.contains('disabled_link') ){

		// 	}
		// }

		

		item.on('click', function() {
			// $(this).closest('.menu').toggleClass('menu_state_open');

		});
	});
})(jQuery);